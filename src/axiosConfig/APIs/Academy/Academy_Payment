import axiosInstance from "../../Instance";

export const academyFeePayment = async (payMethod, params) => {
  try {
    const response = await axiosInstance.get(
      `/method/${payMethod}`,
      {
        params,
      }
    );

    return response.data;
  } catch (error) {
    throw error;
  }
};